# For most projects, this workflow file will not need changing; you simply need
# to commit it to your repository.
#
# You may wish to alter this file to override the set of languages analyzed,
# or to provide custom queries or build logic.
#
# ******** NOTE ********
# We have attempted to detect the languages in your repository. Please check
# the `language` matrix defined below to confirm you have the correct set of
# supported CodeQL languages.
#
name: "CodeQL Advanced"

# CodeQL runs on merges to main and on a weekly schedule, not on every PR.
#
# Rationale:
# - The Swift matrix entry requires `build-mode: manual` (CodeQL's Swift extractor
#   hooks the compiler), so it has to run a full Swift build against the verified,
#   committed FFI on a WarpBuild macOS 15 runner. Every scan performs a clean
#   target build, so superseded pushes are cancelled.
# - The build-integration signal that matters at PR time (does Rust still link
#   with Swift? do offline tests pass?) is already covered by `swift.yml` on
#   every PR. CodeQL Swift adds static-analysis findings, not build verification.
# - Moving CodeQL to push-to-main + weekly cadence keeps the security signal
#   while removing a major source of PR-time CI cost.
on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'LICENSE'
  schedule:
    # Weekly Monday 06:00 UTC: catches new query-pack rules and dependency
    # drift even when nobody has pushed to main.
    - cron: '0 6 * * 1'
  # Manual trigger: re-run between scheduled scans, e.g. after a security-
  # relevant dependency lands and waiting until the next Monday is too long.
  workflow_dispatch: {}

permissions:
  contents: read

# A newer main push supersedes an older main scan. Scheduled and manually
# dispatched scans remain independent and are never cancelled here.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'push' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ matrix.runner }}
    permissions:
      # required for all workflows
      security-events: write

      # required to fetch internal or private CodeQL packs
      packages: read

      # only required for workflows in private repositories
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: actions
          build-mode: none
          runner: warp-ubuntu-latest-x64-16x
        - language: c-cpp
          build-mode: none
          runner: warp-ubuntu-latest-x64-16x
        - language: rust
          build-mode: none
          runner: warp-ubuntu-latest-x64-16x
        - language: swift
          build-mode: manual
          runner: warp-macos-15-arm64-12x
        # CodeQL supports the following values keywords for 'language': 'actions', 'c-cpp', 'csharp', 'go', 'java-kotlin', 'javascript-typescript', 'python', 'ruby', 'rust', 'swift'
        # Use `c-cpp` to analyze code written in C, C++ or both
        # Use 'java-kotlin' to analyze code written in Java, Kotlin or both
        # Use 'javascript-typescript' to analyze code written in JavaScript, TypeScript or both
        # To learn more about changing the languages that are analyzed or customizing the build mode for your analysis,
        # see https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning.
        # If you are analyzing a compiled language, you can modify the 'build-mode' for that language to customize how
        # your codebase is analyzed, see https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/codeql-code-scanning-for-compiled-languages
    steps:
    - name: Checkout repository
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        fetch-depth: 0
        persist-credentials: false

    # --- Swift manual build steps (mirrors swift.yml) ---
    #
    # Swift does not support build-mode: none. Public SDK CodeQL consumes the reviewed, committed
    # FFI artifact from the exact public Rust graph.

    - name: Select Xcode version
      if: matrix.language == 'swift'
      run: sudo xcode-select -s '/Applications/Xcode_16.0.app/Contents/Developer'

    - name: Run network-free retired-engine and dependency gate
      if: matrix.language == 'swift'
      run: ./Scripts/verify-ironwood-static-release-inputs.sh

    - name: Install pinned Rust verification tools
      if: matrix.language == 'swift'
      run: |
        rust_toolchain=$(sed -nE 's/^channel = "([^"]+)"/\1/p' rust-toolchain.toml)
        rustup toolchain install "$rust_toolchain" --profile minimal
        rustup component add --toolchain "$rust_toolchain" llvm-tools-preview

    - name: Verify committed XCFramework
      if: matrix.language == 'swift'
      run: ./Scripts/verify-ironwood-ffi-artifact.sh

    - name: Resolve Swift dependencies
      if: matrix.language == 'swift'
      timeout-minutes: 10
      run: swift package resolve

    # Dependency resolution is intentionally outside the extractor. The explicit
    # arm64 build below keeps CodeQL on the architecture provided by the committed
    # XCFramework instead of allowing a traced compiler process to use x86_64.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@c10b8064de6f491fea524254123dbe5e09572f13 # v4.35.1
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        # If you wish to specify custom queries, you can do so here or in a config file.
        # queries: security-extended,security-and-quality

    - name: Build Swift package
      if: matrix.language == 'swift'
      timeout-minutes: 30
      # Keep the explicit architecture used by the last successful Swift CodeQL
      # build; it also matches this arm64-only runner and FFI slice.
      run: swift build --arch arm64

    # --- End Swift manual build steps ---

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@c10b8064de6f491fea524254123dbe5e09572f13 # v4.35.1
      with:
        category: "/language:${{matrix.language}}"
